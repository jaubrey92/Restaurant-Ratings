<%- include('../partials/header') %>

<% restaurants.forEach(function(r) { %>
  <div><%= r.title %></div>
  <div><%= r.cuisine %></div>
  <div><%= r.price %></div>
<% }); %>

<%- include('../partials/footer') %>