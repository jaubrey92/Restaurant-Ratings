<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <nav>
      <a href="/" <%- title === 'Restaurant Reviews' ? 'class="active"' : '' %>>Home</a>
      <a href="/restaurants" <%- title === 'All Restaurants' ? 'class="active"' : '' %>>All Restaurants</a>
      <% if (user) { %>
        <a href="/restaurants/new" <%- title === 'Add a New Restaurant' ? 'class="active"' : '' %>>Add Restaurant</a>
        <a href="/logout">LOG OUT</a>
      <% } else { %>
        <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
      <% } %>
    </nav>

    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
